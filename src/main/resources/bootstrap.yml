spring:
  application:
    name: license-configuration
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            classpath:/config
server:
  port: 8080
